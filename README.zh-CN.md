<div align="center">

# 🐝 Hivemind

### 多 AI 智能编排平台

统一 10 个 AI Provider，通过单一网关实现智能路由、共享记忆、跨 Agent 知识共享，提供 144 个 API 端点。

[![Version](https://img.shields.io/badge/版本-1.2.0-blue?style=flat-square)](https://github.com/LeoLin990405/Hivemind/releases)
[![Python](https://img.shields.io/badge/Python-3.9+-3776AB?style=flat-square&logo=python&logoColor=white)](https://www.python.org)
[![FastAPI](https://img.shields.io/badge/FastAPI-2.1-009688?style=flat-square&logo=fastapi&logoColor=white)](https://fastapi.tiangolo.com)
[![Tests](https://img.shields.io/badge/测试-195_通过-4cc61e?style=flat-square)]()
[![Endpoints](https://img.shields.io/badge/API-144_端点-orange?style=flat-square)]()
[![License](https://img.shields.io/badge/许可证-AGPL--3.0-purple?style=flat-square)](LICENSE)

**[English](README.md) | [简体中文](README.zh-CN.md)**

[快速开始](#快速开始) &bull; [系统架构](#系统架构) &bull; [桌面客户端](#桌面客户端-hivemindui) &bull; [API 参考](#api-参考) &bull; [CLI 工具](#cli-工具) &bull; [开发路线](#开发路线)

</div>

---

## 什么是 Hivemind？

Hivemind 将 Claude Code 打造为**多 AI 编排中枢**。不再一次只与一个 AI 对话，而是将任务分发给最合适的 Provider — Kimi 处理中文、Codex 做代码审查、Gemini 做前端、DeepSeek 做推理 — 全部通过统一 API，自带自动降级、缓存和记忆注入。

```
你 ──▶ Claude Code ─────────────────────────┐
                                            ├──▶ Hivemind Gateway ──┬──▶ Kimi      (中文, 快速)
你 ──▶ HiveMindUI (桌面端) ──────────────────────┘                        ├──▶ Qwen      (代码, 快速)
                                                                       ├──▶ DeepSeek  (推理)
                                                                       ├──▶ Codex     (代码审查)
                                                                       ├──▶ Gemini    (前端, 多模态)
                                                                       ├──▶ iFlow     (工作流)
                                                                       ├──▶ OpenCode  (多模型)
                                                                       ├──▶ Qoder     (编程)
                                                                       ├──▶ Droid     (Android)
                                                                       └──▶ Antigravity (本地代理)
```

### 关键数据

| 指标 | 数值 |
|------|------|
| AI Provider | **10** 个（9 个远程 + 1 个本地） |
| API 端点 | **144** 个（包含 6 个监控端点） |
| CLI 工具 | **65** 个 |
| 技能集成 | **68** 个 |
| 测试用例 | **195** 个通过 |
| 代码行数 | **52,000+** |

---

## 功能特性

### 智能路由

智能路由器根据关键词规则、性能评分和健康状态，自动为每个任务选择最佳 Provider。

```bash
# 路由器自动决策：React → Gemini, 算法 → Codex, 中文 → Kimi
ccb-submit auto "创建一个 React 登录表单"      # → gemini 3f
ccb-submit auto "分析快速排序时间复杂度"         # → codex o3
ccb-submit auto "用中文解释闭包"                # → kimi
```

内置 12 条路由规则，权重可配置。支持通过 API 添加自定义规则。

### 跨 Agent 共享知识

Agent 将发现的知识发布到共享知识池。任何 Agent 可以一次查询横跨 **Memory V2 + NotebookLM + Obsidian + 共享池** 的所有来源。

```bash
# 发布知识
curl -X POST localhost:8765/api/shared-knowledge/publish \
  -d '{"agent_id":"claude","category":"code_pattern","title":"FastAPI 中间件","content":"..."}'

# 统一跨源查询
curl "localhost:8765/api/shared-knowledge/query?q=React+hooks&sources=memory,shared,notebooklm"
```

置信度评分基于投票/引用/时间衰减因子。FTS5 三元组搜索支持中文文本。

### 统一工具发现

单一索引覆盖本地技能、MCP 工具、MCP 服务器和远程技能 — 支持中英双语关键词匹配。

```bash
curl "localhost:8765/api/tools/search?q=pdf"       # → pdf 技能 (评分 4.5)
curl "localhost:8765/api/tools/search?q=表格"       # → xlsx 技能 (评分 2.5)
curl "localhost:8765/api/tools/search?q=github+pr"  # → MCP create_pull_request
```

### 记忆系统

受认知科学启发的双架构记忆系统：

- **System 1** — 基于 `αR + βI + γT` 评分的快速启发式检索（相关性、重要性、时效性）
- **System 2** — 夜间整合，LLM 驱动的摘要生成
- **可选向量搜索** — sentence-transformers 嵌入，支持 Qdrant/ChromaDB 后端
- **艾宾浩斯衰减** — `T = exp(-0.1 × hours)` 模拟自然遗忘

### 更多功能

| 功能 | 描述 |
|------|------|
| **异步流水线** | 提交 → 轮询 → 获取。非阻塞、并行多 AI 分发 |
| **降级链** | `kimi → qwen → deepseek`，指数退避自动重试 |
| **响应缓存** | 34%+ 命中率，按 Provider TTL。节省 Token 和时间 |
| **健康监控** | 实时 Provider 状态，自动禁用不健康 Provider |
| **多 AI 讨论** | 多 Provider 之间的结构化辩论，基于回合制 |
| **成本追踪** | 按 Provider 的 Token/成本分析，Prometheus 指标 |
| **实时仪表盘** | Web UI `localhost:8765/web`，实时数据统计 |
| **CC Switch** | 热切换 Claude API 端点，无需重启 |
| **知识中枢** | 254+ NotebookLM 笔记本 + Obsidian 仓库集成 |
| **背压控制** | 负载级别监控，过载时拒绝请求 |

---

## 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                          Hivemind Gateway                           │
│                                                                     │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐          │
│  │  智能     │ │  响应     │ │  速率     │ │  健康     │          │
│  │  路由器   │ │  缓存     │ │  限制器   │ │  检查器   │          │
│  └─────┬─────┘ └─────┬─────┘ └─────┬─────┘ └─────┬─────┘          │
│        └──────────────┼─────────────┼─────────────┘                │
│                 ┌─────▼─────────────▼─────┐                        │
│                 │       请求队列          │                        │
│                 └───────────┬──────────────┘                        │
│                             │                                       │
│  ┌──────────────────────────▼──────────────────────────┐           │
│  │                后端执行器                            │           │
│  │   CLI 后端    │  HTTP 后端   │  Pipe 后端           │           │
│  └─────────────────────────────────────────────────────┘           │
│                                                                     │
│  ┌───────────────────┐  ┌────────────────────┐                     │
│  │  Memory V2        │  │  共享知识           │                     │
│  │  (System 1 + 2)   │  │  (跨 Agent)        │                     │
│  │  67 个会话        │  │  FTS5 + 投票       │                     │
│  │  2,582 条消息     │  │  + 统一查询         │                     │
│  └───────────────────┘  └────────────────────┘                     │
│                                                                     │
│  ┌───────────────────┐  ┌────────────────────┐                     │
│  │  知识中枢          │  │  工具路由器        │                     │
│  │  254 个笔记本     │  │  68 个技能已索引   │                     │
│  │  Obsidian 仓库    │  │  双语搜索          │                     │
│  └───────────────────┘  └────────────────────┘                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
        ┌──────────┬───────────┼───────────┬──────────┐
        ▼          ▼           ▼           ▼          ▼
   ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
   │ Kimi 🚀│ │Qwen  🚀│ │Deep  ⚡│ │Codex 🐢│ │Gemini🐢│
   └────────┘ └────────┘ └────────┘ └────────┘ └────────┘
   ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
   │iFlow ⚡│ │OCode ⚡│ │Qoder ⚡│ │Droid ⚡│ │Grav  ⚡│
   └────────┘ └────────┘ └────────┘ └────────┘ └────────┘
```

### Provider 速度分级

| 分级 | Provider | 响应时间 | 最适场景 |
|------|----------|----------|----------|
| 🚀 快速 | Kimi, Qwen | 3–15 秒 | 中文、代码生成、快速问答 |
| ⚡ 中速 | DeepSeek, iFlow, OpenCode, Qoder, Droid | 15–60 秒 | 推理、工作流、多模型 |
| 🐢 慢速 | Codex, Gemini | 60–180 秒 | 代码审查、前端、多模态 |
| 📍 本地 | Antigravity | 不固定 | 本地代理、自定义端点 |

### 数据库架构

| 数据库 | 表数量 | 用途 |
|--------|--------|------|
| `ccb_memory.db` | 13 + 2 FTS5 | 会话、消息、观察、技能追踪 |
| `gateway.db` | 12 + 1 FTS5 | 请求、响应、讨论、成本、共享知识 |
| `knowledge_index.db` | 2 | NotebookLM 索引、查询缓存 |

---

## 桌面客户端 (HiveMindUI)

Hivemind 集成了 [HiveMindUI](https://github.com/Aion-Community/HiveMindUI) 作为桌面 GUI — 基于 Electron + React 19，并原生支持 Hivemind 集成。

### 亮点

- **多 Provider 聊天** — 在聊天界面直接选择 10 个 Provider 中的任意一个
- **流式响应** — 通过 Hivemind Gateway 实时 SSE 流式输出
- **Provider 徽章** — 速度分级（🚀 快速 / ⚡ 中速 / 🐢 慢速）+ 实时延迟显示
- **Gateway 设置** — 配置 Gateway URL、默认 Provider、流式开关
- **Gateway 监控** — 集成监控仪表盘，取代原始 WebUI（已废弃）
  - **📊 Dashboard** — 总览统计、Provider 性能表、24 小时分析
  - **💾 Cache Manager** — 缓存统计 + 一键清除
  - **📋 Task Queue** — 任务列表 + 状态筛选 + 错误追踪
  - **⚙️ Rate Limiting** — 按 Provider 的速率限制状态和重置控制（在设置中）
- **完整 HiveMindUI 功能** — 多 Agent 对话、图片生成、文件管理等

### 快速启动

```bash
cd HiveMindUI
npm install
npm start          # 开发模式（热重载）
npm run build      # 生产构建
```

HiveMindUI 默认连接 `http://localhost:8765`。请先确保 Gateway 已启动：

```bash
python3 -m lib.gateway.gateway_server --port 8765
```

### 访问监控功能

从左侧边栏打开 **Monitor**（监控）访问集成监控仪表盘：

1. **Dashboard**（`/monitor`）— 系统总览，包括：
   - 总请求数、成功率、缓存条目数、命中率
   - Provider 性能表（延迟和成功率）
   - 24 小时请求统计

2. **Cache Manager**（`/monitor/cache`）— 缓存分析，包括：
   - 缓存统计（命中、未命中、清除）
   - 按 Provider 的缓存状态
   - 一键清除缓存（带确认）

3. **Task Queue**（`/monitor/tasks`）— 任务管理，包括：
   - 实时任务列表 + 状态筛选
   - 任务详情：ID、状态、Provider、时间戳、错误
   - 筛选：全部、待处理、已完成、失败

4. **Rate Limiting**（设置 → Hivemind → Rate Limit）— 速率限制控制：
   - 按 Provider 的速率限制状态（剩余/总量）
   - 单个 Provider 的重置按钮
   - 实时状态更新

> **注意**: 原始独立 WebUI（`lib/web_server.py`，端口 8080）已**废弃**，由 HiveMindUI 的集成监控替代。详见 `deprecated/README.md`。

### 架构

```
┌──────────────────────────────────────┐
│         HiveMindUI (Electron)            │
│                                      │
│  ┌────────────┐  ┌───────────────┐   │
│  │ React UI   │  │ Hivemind      │   │
│  │ (渲染进程)  │──│ Agent 插件    │   │
│  └────────────┘  └───────┬───────┘   │
│                          │           │
│  ┌───────────────────────▼────────┐  │
│  │  HivemindConnection            │  │
│  │  HTTP + SSE 流式客户端         │  │
│  └───────────────────────┬────────┘  │
└──────────────────────────┼───────────┘
                           │ HTTP/SSE
                           ▼
              ┌─────────────────────┐
              │  Hivemind Gateway   │
              │  localhost:8765     │
              └─────────────────────┘
```

### Hivemind 集成文件

| 组件 | 文件 | 用途 |
|------|------|------|
| **核心 Agent** | | |
| 类型定义 | `src/agent/hivemind/types.ts` | Provider 选项、速度分级、配置类型 |
| 连接层 | `src/agent/hivemind/HivemindConnection.ts` | Gateway HTTP + SSE 客户端 |
| 适配器 | `src/agent/hivemind/HivemindAdapter.ts` | Gateway 响应 → HiveMindUI 消息格式转换 |
| Agent | `src/agent/hivemind/index.ts` | HivemindAgent 主类 |
| 管理器 | `src/process/task/HivemindAgentManager.ts` | 会话生命周期管理 |
| **聊天 UI** | | |
| 聊天 UI | `src/renderer/.../HivemindChat.tsx` | 聊天容器组件 |
| 输入框 | `src/renderer/.../HivemindSendBox.tsx` | 输入框 + Provider 选择器 |
| 徽章 | `src/renderer/.../HivemindProviderBadge.tsx` | Provider 速度分级徽章 |
| 路由信息 | `src/renderer/.../HivemindRoutingInfo.tsx` | 路由状态指示器 |
| 设置面板 | `src/renderer/.../HivemindModalContent.tsx` | Gateway 配置面板 |
| **监控 (v1.2)** | | |
| 服务层 | `src/renderer/services/GatewayMonitorService.ts` | 监控 API 客户端（98 行）|
| Hook | `src/renderer/hooks/useGatewayStats.ts` | 统计管理 Hook（68 行）|
| 布局 | `src/renderer/pages/monitor/MonitorLayout.tsx` | 监控布局 + 导航 |
| Dashboard | `src/renderer/pages/monitor/Dashboard.tsx` | 总览 + Provider 性能 |
| Cache Manager | `src/renderer/pages/monitor/CacheManager.tsx` | 缓存统计 + 清除 |
| Task Queue | `src/renderer/pages/monitor/TaskQueue.tsx` | 任务列表 + 筛选 |
| Rate Limit | `src/renderer/.../RateLimitControl.tsx` | 速率限制控制（113 行）|
| Worker | `src/worker/hivemind.ts` | Worker 存根 |

> **许可证**: HiveMindUI 采用 Apache-2.0 许可证。详见 `HiveMindUI/LICENSE`。


## 快速开始

### 前置条件

- Python 3.9+
- 至少配置一个 AI Provider（推荐 Kimi 或 Qwen，最快上手）

### 安装

```bash
git clone https://github.com/LeoLin990405/Hivemind.git
cd Hivemind
pip install fastapi uvicorn aiohttp
```

### 启动 Gateway

```bash
python3 -m lib.gateway.gateway_server --port 8765
```

### 验证

```bash
# 健康检查
curl localhost:8765/api/health
# → {"status":"ok"}

# 列出 Provider
curl localhost:8765/api/providers
# → 10 个 Provider 及其优先级

# 智能路由测试
curl -X POST "localhost:8765/api/route?message=写一个Python脚本"
# → {"provider":"qwen","confidence":0.45,"rule_description":"Python and scripting"}
```

### 发送第一个请求

```bash
# 异步模式（推荐）
ccb-submit kimi "JavaScript 中什么是闭包？"
# → 立即返回 request_id

# 查询结果
ccb-query get <request_id>

# 同步模式（仅用于快速 Provider）
ccb-cli kimi "1+1=?"
```

---

## API 参考

**基础 URL:** `http://localhost:8765`

### 核心

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/ask` | 向 Provider 发送请求 |
| POST | `/api/ask/stream` | SSE 流式响应 |
| POST | `/api/route` | 获取路由推荐 |
| GET | `/api/route/rules` | 列出路由规则 |
| GET | `/api/router/config` | 路由器配置 |
| GET | `/api/router/scores` | Provider 性能评分 |
| GET | `/api/providers` | 列出所有 Provider |
| GET | `/api/status` | Gateway 状态 |
| GET | `/api/health` | 健康检查 |

### 监控 (v1.2)

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/monitor/stats` | 性能统计（24 小时窗口）|
| GET | `/api/monitor/cache/stats` | 缓存分析（命中、未命中、清除）|
| POST | `/api/monitor/cache/clear` | 清除缓存（可选 Provider 过滤）|
| GET | `/api/monitor/tasks` | 任务队列列表 + 状态筛选 |
| GET | `/api/monitor/ratelimit` | 按 Provider 的速率限制状态 |
| POST | `/api/monitor/ratelimit/{provider}/reset` | 重置指定 Provider 的速率限制 |

### 共享知识 (v1.1)

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/api/shared-knowledge/publish` | 发布知识条目 |
| GET | `/api/shared-knowledge/query?q=` | 统一跨源查询 |
| POST | `/api/shared-knowledge/vote` | 投票（赞同/反对/引用） |
| GET | `/api/shared-knowledge/feed` | 浏览知识条目 |
| GET | `/api/shared-knowledge/stats` | 知识统计 |
| DELETE | `/api/shared-knowledge/{id}` | 删除条目 |

### 工具路由器 (v1.1)

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/tools/search?q=` | 搜索工具（双语） |
| GET | `/api/tools/index` | 索引统计 |
| POST | `/api/tools/rebuild` | 重建索引 |
| GET | `/api/tools` | 列出所有工具 |

### 记忆系统

| 方法 | 端点 | 描述 |
|------|------|------|
| GET | `/api/memory/search?query=` | 搜索对话记录 |
| GET | `/api/memory/sessions` | 列出会话 |
| GET | `/api/memory/stats` | 记忆统计 |
| POST | `/api/memory/add` | 添加观察 |
| GET | `/api/memory/observations` | 列出观察 |

### 更多端点

| 分类 | 数量 | 前缀 |
|------|------|------|
| 监控 (v1.2) | 6 | `/api/monitor/*` |
| 批量操作 | 5 | `/api/batch/*` |
| 多 AI 讨论 | 12 | `/api/discussion/*` |
| 管理后台 | 9 | `/api/admin/*` |
| 缓存管理 | 4 | `/api/cache/*` |
| 成本分析 | 5 | `/api/costs/*` |
| 健康检查器 | 5 | `/api/health-checker/*` |
| 数据导出 | 3 | `/api/export/*` |
| 知识中枢 | 8 | `/knowledge/*` |
| 流式传输 | 5 | `/api/stream/*` |
| CC Switch | 4 | `/api/cc-switch/*` |
| 运行时 | 8 | 多个 |
| Prometheus | 1 | `/metrics` |
| Web UI | 2 | `/web` |

**完整 OpenAPI 文档:** `http://localhost:8765/docs`

---

## CLI 工具

### 主要命令

```bash
ccb-submit <provider> "消息"    # 异步提交（推荐）
ccb-query get <id>              # 获取已完成的结果
ccb-query status <id>           # 查询请求状态
ccb-cli <provider> "消息"       # 同步调用（快速 Provider）
ccb-poll <id1> <id2> ...        # 轮询多个请求
```

### Provider 快捷命令

每个 Provider 都有 `ask` / `pend` / `ping` 快捷方式：

| Provider | Ask | Pend | Ping |
|----------|-----|------|------|
| Kimi | `kask` | `kpend` | `kping` |
| Qwen | `qask` | `qpend` | `qping` |
| DeepSeek | `dask` | `dpend` | `dping` |
| Gemini | `gask` | `gpend` | `gping` |
| Codex | `cask` | `cpend` | `cping` |
| OpenCode | `oask` | `opend` | `oping` |
| iFlow | `iask` | `ipend` | `iping` |

### 管理工具

```bash
ccb-gateway                    # 启动/管理 Gateway
ccb-monitor                    # 实时监控
ccb-stats                      # 使用统计
ccb-cache                      # 缓存管理
ccb-cc-switch                  # 切换 Claude 端点
ccb-mem                        # 记忆操作
ccb-knowledge                  # 知识中枢 CLI
```

### 模型快捷方式

```bash
ccb-cli codex o3 "..."         # Codex + o3（深度推理）
ccb-cli codex o4-mini "..."    # Codex + o4-mini（快速）
ccb-cli codex gpt-4o "..."     # Codex + GPT-4o（多模态）
ccb-cli gemini 3f "..."        # Gemini 3 Flash
ccb-cli gemini 3p "..."        # Gemini 3 Pro
ccb-cli deepseek reasoner "."  # DeepSeek R1
ccb-cli kimi thinking "..."    # Kimi 思考链模式
```

---

## 项目结构

```
Hivemind/
├── HiveMindUI/                    # 桌面客户端 (Electron + React 19)
│   ├── src/
│   │   ├── agent/hivemind/    # Hivemind Gateway 客户端
│   │   ├── renderer/          # React UI（含 Hivemind 组件）
│   │   │   ├── pages/monitor/ # 监控页面（Dashboard、Cache、Tasks）
│   │   │   ├── services/      # GatewayMonitorService
│   │   │   └── hooks/         # useGatewayStats
│   │   └── process/           # 进程管理
│   ├── package.json
│   └── forge.config.ts
├── bin/                        # 65 个 CLI 工具
├── lib/
│   ├── common/                 # 共享工具（日志、错误、认证）
│   ├── gateway/
│   │   ├── app.py              # FastAPI 应用工厂
│   │   ├── router.py           # 智能路由引擎
│   │   ├── routes/             # 20 个路由模块（144 个端点）
│   │   │   └── monitor.py      # 监控 API 端点 (v1.2)
│   │   ├── middleware/         # 记忆注入中间件
│   │   ├── backends/           # CLI、HTTP、Pipe 执行器
│   │   └── ...
│   ├── memory/                 # Memory V2（System 1 + 2）
│   ├── knowledge/              # NotebookLM + Obsidian + 共享知识
│   ├── providers/              # 10 个 Provider 适配器 + BaseCommReader
│   └── skills/                 # 技能发现 + 工具索引
├── tests/                      # 195 个测试用例
├── docs/                       # 架构与路线图文档
│   ├── MIGRATION_TO_HIVEMINDUI.md  # WebUI 迁移指南
│   ├── HIVEMINDUI_VS_WEBUI_COMPARISON.md  # 功能对比
│   └── WEBUI_REPLACEMENT_STATUS.md    # 替换验证状态
├── deprecated/                 # 已废弃的原始 WebUI
│   ├── web_server.py.deprecated
│   ├── web_server_template.py.deprecated
│   └── README.md               # 废弃说明
├── mcp/                        # MCP 服务器集成
└── screenshots/                # 演示资源
```

---

## 开发路线

| 版本 | 状态 | 亮点 |
|------|------|------|
| v0.26 | ✅ 完成 | 知识中枢、10 个 Provider、Web UI |
| v1.0 | ✅ 完成 | 模块化重构、19 个路由模块、BaseCommReader |
| v1.1 | ✅ 完成 | 共享知识、工具路由器、统一查询 |
| **v1.2** | **✅ 当前** | **HiveMindUI 桌面客户端、Hivemind Agent 集成、监控仪表盘（取代 WebUI）、6 个监控 API 端点、数据库 Schema v13** |
| v1.3 | 计划中 | 向量语义搜索、jieba 分词 |

---

## 贡献指南

Hivemind 由人类与 AI 协作构建，欢迎贡献。

1. Fork 本仓库
2. 创建特性分支（`git checkout -b feat/my-feature`）
3. 运行测试（`python3 -m pytest tests/ -x -q`）
4. 提交并推送
5. 发起 Pull Request

---

## AI 协作者

本项目由多个 AI 系统协作贡献：

| AI | 角色 | 贡献 |
|----|------|------|
| **Claude** | 架构师与编排者 | 核心设计、记忆系统、测试、文档 |
| **Codex** | 代码工程师 | v1.0 重构、v1.1 实现、v1.2 HiveMindUI 集成 |
| **Kimi** | 中文专家 | 中文 NLP、Ollama 集成、国际化 |
| **DeepSeek** | 推理引擎 | 算法设计、评分公式 |
| **Gemini** | 前端与分析 | Web UI、多模态分析 |
| **Qwen** | 代码生成器 | Provider 适配器、CLI 工具 |

---

## 许可证

[AGPL-3.0](LICENSE) — 自由使用、修改和分发。网络使用需公开源代码。

---

<div align="center">

**[文档](docs/)** &bull; **[API 文档](http://localhost:8765/docs)** &bull; **[Web 仪表盘](http://localhost:8765/web)** &bull; **[Issues](https://github.com/LeoLin990405/Hivemind/issues)**

由多个 AI 协作构建，如同蜂巢思维。

</div>
